package com.companies.measurementapp

import androidx.compose.ui.graphics.Color

enum class ColorSelection(val colors: Color) {
    Yellow(colors = Color.Yellow),
    Orange(colors = Color.Cyan),
    Purple(colors = Color.Magenta),
    Pink(colors = Color.Red),
    Green(colors = Color.Green)
}